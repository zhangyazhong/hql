package com.github.zhangyazhong.hql.query;

//import org.hibernate.Query;

import java.util.Map;

/**
 * @author zhangyazhong
 * @version 1.0
 */
public class HqlQueryUtil {
    /*
    public static Query injectParameters(Query query, Map<Integer, String> params) {
        params.forEach(query::setString);
        return query;
    }
    public static Query setLimit(Query query, int start, int limit) {
        query.setFirstResult(start);
        query.setMaxResults(limit);
        return query;
    }
    */
}
